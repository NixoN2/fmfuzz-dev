name: Bitwuzla Coverage Daily Check

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  check:
    uses: ./.github/workflows/coverage-daily-check.yml
    with:
      solver: bitwuzla
    secrets: inherit
